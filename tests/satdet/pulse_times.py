import numpy as np
import json
import matplotlib.pyplot as plt


'''
The idea of this test is to make sure that the offset generated by a pulse does not depend on how it is chunked
In other words, the same pass evaluated at different start and end times should not yield different offsets/different detections
This is tested using the same pulse, but evaluated using three different start and end times

At the moment this is slightly difficult to test, but if we set the batch up such that it's the exact pulse times we should be okay?
'''